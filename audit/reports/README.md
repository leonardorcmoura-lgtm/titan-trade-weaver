# Audit Reports

Reconciliation reports written by `scripts/verify-integrity.ts` and
manifesto rebuilds written by `scripts/rebuild-manifesto.ts`.

Naming: `<kind>-<timestamp>.json`. Append-only — never overwrite history.
